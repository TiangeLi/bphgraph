{
    "version": 2,
    "builds": [
      { "src": "langserver.py", "use": "@vercel/python" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/langserver.py" }
    ],
    "env": {
      "APP_MODULE": "langserver:app"
    }
  }